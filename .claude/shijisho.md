# 指示書

完了した項目にはチェックを付けてください。矛盾点があれば必ず質問してから進めてください。

## 概要

このプロジェクトは、かつてVite+Vikeで開発していたWebアプリケーションです。VikeのSSGに関する情報があまりに少なかったためNext.jsに移管しましたが、Next.jsはi18nとSSGを同時に使用できない仕様があるという難点を抱えています。そのため、このプロジェクトの主要な目標は「Next.jsからAstroに移管し、さらにSSGする」ことになります。

このサイトでは、駅名標を「手動で生成」または「路線を作って生成」することができます。すべての作業はローカルで完結し、アカウント連携機能などは実装しません。生成したSQLiteファイルは、ユーザーが必要に応じてダウンロードすることができます。

## 実装1：フレームワークとUIのマイグレーション

以下の手順で、すべて実装してください。

1. [x] まずは外見を保ったまま、このNext.jsのプロジェクトをAstroに移植してください。
2. [x] MUIはAstroで使用するとFOUCを起こします。できるだけ外見を保ちつつ、MUIからMantineに移行してください。不足するUIがある場合は、そのコンポーネントに対する代案をまず提案してください。
3. [x] 翻訳関数が破損しているかもしれません。必要に応じて正常動作するように書き換えしてください。
4. [x] 全てのページで、指定の言語でリンクカードが表示される状態になっていることを確認してください。そうなっていない場合は、実装してください。今後更に言語を追加する可能性もあるため、ビルド時に生成するようにしてください。ただし、クライアントJavaScriptでリンクカード情報をインジェクトするとクローラーが認識できないため、そうならないように留意してください。

## 実装2：データベースのリライト準備

1. [x] 現在はJSONオブジェクトをpopulateして内部計算をしています。この構造を読み取って解析してください。
2. [x] JSONオブジェクトをpopulateする形式には、持続性や私たちから見た処理の簡便さにかなり問題があります。そのため、SQLiteを用いた実装方針に変更します。
   1. [x] 以下の作業に当たって、データはサーバーには一切保持しないことに留意してください。ユーザーはSQLiteファイルを必要に応じてエクスポートできますが、それは後で実装することとします。
   2. [x] 現在の構造を保ちながらRDBを設計し、概念図を @.claude/output ディレクトリを作成してそこに出力してください。

## 実装3：UX改善（2026.3.0-alpha2アップデート内容）

1. [x] 言語を変更したときのページ遷移をスムーズにしてください。また、フォームの入力内容をすべて保持してください。
2. [x] SessionStorageを使用して、「手動で生成」の入力内容をすべて保持するようにしてください。また、「左右を入れ替える」ボタンの右に「（ゴミ箱アイコン）入力内容をリセット」のボタンを作り、押下時にMantineのモーダル（なければ作成してください）で警告を出すようにしてください。
3. [x] ヘッダーに、「ライト・ダークテーマ切り替え」を実装してください。不足しているCSSプロパティも新規に作成してください。デフォルトはユーザーのブラウザ指定テーマになるようにしてください。
4. [x] 初期値が「あいうえお」「かきくけこ」となっているテキストフィールドは、テスト用のものです。削除してください。
5. [x] カラーピッカーが画面全体に広がって不便です。また、一番下までスライドして黒色を選ぶと下側のカラーバーが左に戻ってしまいます。他のカラーピッカーを選定するか、Mantineにあればそれを採用してください。
6. [x] フォントの読み込みが完了するまでロード画面アニメーションを再生してください。

## 実装4：データベース実装（2026.3.0-alpha4アップデート内容）

1. [x] まず、 @.claude/output/er-diagram.mmdを読み、データベースの設計図を理解してください。
2. [x] すでにあるデータベース関連ファイルを、すべてer-diagram.mmdの内容に合わせて上書きしてください。
3. [x] ページのタブに「シンプル入力」と「路線から入力」と「路線を編集」の3つのタブを追加し、現在のsimpleinputおよびJSONファイル構造はすべて「シンプル入力」に移管してください。
4. [x] 先に「路線を編集」画面を作成し、会社、路線、駅のデータを管理できるようにしてください。この画面では、プレビューを出す必要はありません。
5. [x] 「路線を編集」画面が完成したら、このタブで作ったSQLiteファイルのダウンロードボタンを作成してください。
6. [x] 「路線を編集」で入力した内容は、LocalStorageなどを使用して再読み込みをしても保持するようにしてください。
7. [x] その後、下準備として「JR東日本形式はこのデータを必要とする」という内容を整理してください。
8. [x] 「路線を編集」画面が完成したら、「路線から入力」タブの内容を作成してください。現在はJR東日本形式しかないため、スタイル選択のプルダウンには「JR東日本風」のみを入れてください。
   1. [x] 「路線から入力」では、今の駅をユーザーが選択すればその隣の駅は自ずとわかります。そのため、「駅名標の方向」「表示する路線」という形式でユーザーが簡単に駅を選択できるようにしてください。

## 実装5：データベースと直接入力の整合性・拡張性確保

「 @.claude/output/er-diagram.mmd の内容が中心の設計図となる構成であり、今後追加するあらゆる駅名標生成に必要なデータがすべて入っている」ことを認識し、以下の作業に取り掛かってください。

1. [x] 「JR東日本形式」がデータベースに保存してある内容から、特にどのプロパティを必要とするか、配列を含む場合の上限はいくらであるかなどをTypeScriptの方式で宣言してください。
2. [x] 「シンプル入力」にはすべての情報を完備しつつ、現在選択している形式が必要としている情報のみをフォームに入力可能として表示してください。
   1. [x] 例えば、JR東日本形式の場合、「当駅情報」「右駅情報」「左駅情報」「会社情報：JR東日本であることとその色の情報が必要です」「路線情報：路線と色情報は当駅、進行方向駅の駅ナンバリングの枠線の色それぞれに直結します」ごとに、データベースのどの値が必要になるかということから項目名を決定し、類推し、形式を合わせてきれいにしてください。
3. [x] シンプル入力で、左右入れ替えボタンを押すとクラッシュします。修正してください。
4. [x] キャッシュに破損したデータが入っている場合に、ページ全体が閲覧不能になります。破損したキャッシュが原因でページが開けない場合、警告して削除を促してください。

## 実装6：データベース改良（2026.3.0-alpha4アップデート内容）

1. [x] 駅及び路線の内部IDは、新規作成時においてはUUIDv7を使用していることを確認してください。もし未実装の場合、必要に応じてパッケージも追加してください。サンプルデータに対しては置換をする必要はありません。
2. [x] データベースに、以下の属性を追加してください。
   1. [x] 路線に対し、「環状線であるかどうか」のBoolean値。
   2. [x] 「特定都区市内」テーブルの分離。「名称（Text）」「略称（Text：一文字のみ）」「黒塗りするか（Boolean）」
   3. [x] 駅に対し、特定都区市内は特定都区市内テーブルから選択するように改良。フロントエンドもそれに対応するように変更。
3. [x] サンプルデータを以下の要件に従うように更新してください。
   1. [x] JR東日本の会社の色はデフォルトの色（#3a9200）と一致させてください。
   2. [x] 山手線はすべての駅の特定都区市内が「山（山手線内：白塗り）」と「区（東京23区内：黒塗り）」です。
   3. [x] 山手線は環状線です。
4. [x] DB_VERSION を0.0.2または適切なバージョンにアップデートしてください。まだリリース完了段階ではないので、誤ってメジャーバージョンを上げることはしないでください。

## 実装7：駅の重複ハンドリング（2026.3.0-alpha8アップデート内容）

1. [x] 山手線と京浜東北線が代表例になるように、重複する駅を走る別の路線があります。現行では、この状態に回答した路線を編集する画面がありません。
   1. [x] 路線に駅を追加するとき、「すでにある駅から追加する」ボタンを実装してください。このとき、現在「駅を追加」にある項目の中で「駅番号」以外の項目をすべて引き継いでください。
   2. [x] すでにある駅から追加するとき、複製ではなく同一のIDを使用してください。これは、駅名標描画フェーズで「この駅・次の駅・前の駅」がどの路線に属するかを一意に特定する必要があるためです。例えば、「東京駅は山手線に所属する01番の駅であり、さらに京浜東北線に所属する26番の駅でもある」という情報は駅名標描画に不可欠なものになります。
   3. [x] つまりこれは、路線によって同じ駅に割り当てる駅番号が異なるということを意味しています。現在のデータベーススキーマでこれを対処できるか判定し、対処できない場合は中間テーブルなどを追加してください。
2. [x] 上記の作業が完了したら、山手線のサンプルデータに京浜東北線・根岸線（全駅）も追加してください。

## 実装8：他のスタイルサポート（2026.3.0-beta2アップデート内容）

1. [x] シンプル入力タブ使用中に、ページを離れると選択しているスタイルも初期化されてしまいます。保持するようにしてください。
2. [x] JR西日本形式は、signStyles.tsが定義しているように、以下のデータを要求しません。シンプル入力モードでこれらを非表示にしてください。
   1. [x] ratio（が変更不能なものがあります。シンプル入力・路線から入力のいずれにおいても変更不能な場合は隠してください）
   2. [x] 中央四角の色
   3. [x] 路線追加ボタン（路線の上限は1で、最低値も1です）
   4. [x] 会社カラー
3. [x] JR西日本形式を選択しているとき、画像の大きさを選択する選択肢の比率が実際のサイズを反映していません。修正してください。ratioが変更不能な形式が今後さらに増えることも想定して実装してください。
4. [x] JrWestSign.tsxには冗長な記述がいくつかあります。できるだけ簡便になるようにコード全体をリファクタしてください。

## 実装9：駅番号表示の会社別実装

この実装では、「駅番号の表示（JY17など）」が駅名標スタイルに依存している現在の状態から、現実に即して、鉄道会社のスタイルに依存するようにコードを修正します。

1. [x] データベースの会社に「駅番号スタイル」列を追加してください。
2. [x] 現在は、JR東日本風のバッジしかありません。そのため、まずはJR東日本スタイルからどの部分が駅番号（およびスリーレターコード）を描画しているか取得し、暫定的に「JR東日本風」の1種類のみ追加してください。
3. [x] 現在のスリーレターコード描画は、今後追加するどのタイプの駅番号の表示とも互換性がありません。スリーレターコードは、JR東日本の駅名標部分のみをつなげて描画するようにしてください。

## 実装10：（2026.3.0-beta16アップデート内容）

1. [x] 「路線から入力」モードの「路線図」モードには、以下の問題があります。すべて修正してください。
   1. [x] 駅番号バッジが左上を基点として小さくなっています。中心を基点として小さくなるようにしてください。
   2. [x] 「通過駅」オプションのトグルスイッチを、「表示（半透明）」「非表示（余白）」「非表示（切り詰め）」の3択スイッチに変更してください。
   3. [x] スリーレターコードを含んだ通過駅が、他の駅番号バッジと異なり灰色が重なっています。原因を見つけて修正し、上に白い図形を被せる方法で修正を試してみてください。他の方法で解決しても構いませんが、単純に不透明度を使うと線が重複して透明度が異なってしまう問題が起こることに注意してください。
2. [x] @.claude/output/er-diagram.mmd を最新の状態と合致するように修正してください。また、今後データベースをアップデートするときは常にこのファイルもアップデートするようにしてください。
3. [x] サンプルデータを生成するpythonスクリプトを、以下の要領でアップデートしてください。
   1. [x] データベースのバージョンが変化しています。スキーマを合わせてください。
   2. [x] 京浜東北線には「普通」の他に「快速」があり、新橋、有楽町、鶯谷、日暮里、西日暮里を通過します。また、御徒町が特殊状態です。serviceテーブルがそういった形になるようにしてください。
   3. [x] このファイル末尾の「参考資料」を読みながら、湘南新宿ラインを新規に追加してください。

## 実装11：不具合修正

1. [x] 路線図モードにおいて、種別の選択が1つで通過する駅がある場合に、次の不具合を修正してください。
   1. [x] 横向きで駅を上（縦書き）または下（縦書き）にし、かつ「ドットを置換」モードのとき、通過する停車しない駅の駅番号バッジが駅名の横に来るようになっています。上（縦書き）の場合は駅名の下に、下（縦書き）の場合は駅名の上になるように修正してください。このとき、路線を示す線の上に駅番号バッジが来ないようにしてください。
   2. [x] 縦向きかつ「ドットを置換」モードのときのとき、
      1. [x] 駅名の側が右のときに駅番号バッジが路線を示す線の上に被っています。通過する駅名も含めて右にずらしてください。
      2. [x] 駅名の側が左のとき、駅番号バッジが変なところに出ています。駅名の右に出るように修正してください。その後、通過する駅と駅番号バッジを左にずらしてください。

## 実装12：不具合修正2

1. [x] 路線図モードにおいて、種別の選択が1つかつ通過する駅がある場合に、次の不具合を修正してください。
   1. [x] 駅番号の表示を「バッジ」にしたときに、駅番号バッジが半透明になるようにしていますが、これを半透明にならないようにしてください。
   2. [x] すべての駅に停車しない種別の選択を1つでもした場合は、駅名スタイル「標準」を選択不能にしてください。
2. [x] 路線図モードにおいて、種別の選択が1つのとき、路線名の右に種別を書いてください。
3. [x] 路線図モードにおいて、設定項目がかなり散らかってしまっています。全体的にデザインの見直しを行ってください。

## 参考資料

### 湘南新宿ライン

- 路線カラー：#c61c1b
- 略称：JS
- 種別：普通（#2b5152）、快速（宇都宮線内）（#2e5500）、快速（横須賀線・品鶴戦内）（#3b4000）、特別快速（#491615）
- 駅、停車種別一覧：
  - JS06　逗子　普通、快速（宇）
  - JS07　鎌倉　普通、快速（宇）
  - JS08　北鎌倉　普通、快速（宇）
  - JS09　大船　普通、快速（宇）、快速（横）、特快　スリーレターコード「OFN」
  - JS10　戸塚　普通、快速（宇）、快速（横）、特快　スリーレターコード「TTK」　特定都区市内：浜
  - JS11　東戸塚　普通、快速（宇）　特定都区市内：浜
  - JS12　保土ヶ谷　普通、快速（宇）　特定都区市内：浜
  - JS13　横浜　普通、快速（宇）、快速（横）、特快　スリーレターコード「YHM」　特定都区市内：浜
  - JS14　新川崎　普通、快速（宇）
  - JS15　武蔵小杉　普通、快速（宇）、快速（横）、特快　スリーレターコード「MKG」
  - JS16　西大井　普通、快速（宇）　特定都区市内：区
  - JS17　大崎　普通、快速（宇）、快速（横）、特快　スリーレターコード「OSK」　特定都区市内：山、区
  - JS18　恵比寿　普通、快速（宇）、快速（横）　スリーレターコード「EBS」　特定都区市内：山、区
  - JS19　渋谷　普通、快速（宇）、快速（横）、特快　スリーレターコード「SBY」　特定都区市内：山、区
  - JS20　新宿　普通、快速（宇）、快速（横）、特快　スリーレターコード「SJK」　特定都区市内：山、区
  - JS21　池袋　普通、快速（宇）、快速（横）、特快　スリーレターコード「IKB」　特定都区市内：山、区
  - JS22　赤羽　普通、快速（宇）、快速（横）、特快　スリーレターコード「ABN」　特定都区市内：区
  - JS23　浦和　普通、快速（宇）、快速（横）、特快　スリーレターコード「URW」
  - JS24　大宮　普通、快速（宇）、快速（横）、特快　スリーレターコード「OMY」
- 特殊停車駅：なし